# Some documents are still being organized, and I will continue to provide updates...

## SEINE
## Installation

1. Install MMCV-full (Linux recommend): `pip install MMCV-full==1.3.13`;
2. Install requirements package: `pip install -r requirements.txt`;
3. Download SEINE: `git clone https://github.com/zhangye_zoe/SEINE`;
4. Install SEINE: `pip install -e .`;
